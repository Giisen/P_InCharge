events { }
http {
    include mime.types;

    server {
        listen 80;

        location / {
            root /home/krister/docker-nginx/html;
            try_files $uri $uri/ /index.html =404;
        }
    }
}